---
phase: 03-diffmanager-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - graph_lineage/config_file/commit_msg/__init__.py
  - graph_lineage/config_file/commit_msg/lineage_messages.yml
  - graph_lineage/config_file/commit_msg/loader.py
  - graph_lineage/diff/reconstructor.py
  - graph_lineage/diff/description.py
  - tests/test_messages.py
  - tests/test_reconstructor.py
autonomous: true
requirements: [R3.2+]

must_haves:
  truths:
    - "YML message templates load from bundled file with user override support"
    - "Description messages auto-generated from changed files list and strategy"
    - "Codebase can be reconstructed from lineage chain (base snapshot + sequential diffs)"
    - "RETRY and RESUME strategies produce correct template messages"
    - "Round-trip test passes: snapshot -> diff -> reconstruct == original"
  artifacts:
    - path: "graph_lineage/config_file/commit_msg/lineage_messages.yml"
      provides: "Default message templates and critical files list"
      contains: "file_modified"
    - path: "graph_lineage/config_file/commit_msg/loader.py"
      provides: "YML loader with user override"
      exports: ["load_messages"]
    - path: "graph_lineage/diff/description.py"
      provides: "Description message generation"
      exports: ["generate_description"]
    - path: "graph_lineage/diff/reconstructor.py"
      provides: "Codebase reconstruction from lineage chain"
      exports: ["reconstruct_codebase", "apply_unified_diff"]
  key_links:
    - from: "graph_lineage/diff/description.py"
      to: "graph_lineage/config_file/commit_msg/loader.py"
      via: "imports load_messages"
      pattern: "from.*loader.*import.*load_messages"
    - from: "graph_lineage/diff/reconstructor.py"
      to: "graph_lineage/diff/differ.py"
      via: "uses unified diff format produced by differ"
      pattern: "apply_unified_diff"
---

<objective>
Implement message generation (YML-configurable descriptions) and codebase reconstruction from lineage chain -- completing the DiffManager system.

Purpose: Enable auto-generated experiment descriptions and the ability to reconstruct codebase state at any point in the lineage history.
Output: Message loader + templates, description generator, and chain reconstructor with full round-trip test coverage.
</objective>

<execution_context>
@/Users/T.Finizzi/repo/FineTuning-Lineage/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T.Finizzi/repo/FineTuning-Lineage/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-diffmanager-system/03-CONTEXT.md
@.planning/phases/03-diffmanager-system/03-RESEARCH.md
@.planning/phases/03-diffmanager-system/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From graph_lineage/diff/snapshot.py:
```python
CRITICAL_FILES = ["config.yaml", "prepare.py", "train.py", "requirements.txt"]

class CodebaseSnapshot(BaseModel):
    model_config = {"frozen": True}
    files: dict[str, str] = Field(default_factory=dict)
    def file_hash(self, filename: str) -> str: ...
    def hashes(self) -> dict[str, str]: ...

def capture_snapshot(codebase_root: Path) -> CodebaseSnapshot: ...
```

From graph_lineage/diff/differ.py:
```python
def compute_file_hash(content: str) -> str: ...
def compute_unified_diff(old_content: str, new_content: str, filename: str) -> str: ...
def detect_changes(old_hashes: dict[str, str], new_hashes: dict[str, str]) -> list[str]: ...
def compute_snapshot_diff(old_snapshot: CodebaseSnapshot, new_snapshot: CodebaseSnapshot) -> dict[str, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YML message loader + description generator</name>
  <files>graph_lineage/config_file/commit_msg/__init__.py, graph_lineage/config_file/commit_msg/lineage_messages.yml, graph_lineage/config_file/commit_msg/loader.py, graph_lineage/diff/description.py, tests/test_messages.py</files>
  <read_first>
    - .planning/phases/03-diffmanager-system/03-CONTEXT.md
    - .planning/phases/03-diffmanager-system/03-RESEARCH.md
    - graph_lineage/diff/__init__.py
  </read_first>
  <behavior>
    test_messages.py:
    - Test 1: `load_messages()` returns dict with keys: file_modified, no_changes, non_critical_changes, retry, resume, critical_files
    - Test 2: `load_messages(user_path)` loads from user-provided YML when file exists
    - Test 3: `load_messages(user_path)` falls back to default when user file does not exist
    - Test 4: `generate_description(changed_files=["train.py"], strategy="BRANCH", ...)` -> "train.py modified"
    - Test 5: `generate_description(changed_files=["train.py", "config.yaml"], strategy="BRANCH", ...)` -> "config.yaml modified, train.py modified" (or similar concatenation)
    - Test 6: `generate_description(changed_files=[], strategy="BRANCH", ...)` -> "no codebase changes"
    - Test 7: `generate_description(changed_files=["other.py"], strategy="BRANCH", ...)` -> "codebase changes, but not in critical files"
    - Test 8: `generate_description(strategy="RETRY", exp_id="e-001", ...)` -> "RETRY FROM e-001"
    - Test 9: `generate_description(strategy="RESUME", exp_id="e-001", ckp_id="c-001", ...)` -> "RESUME FROM e-001, checkpoint c-001"
  </behavior>
  <action>
    1. Create `graph_lineage/config_file/commit_msg/__init__.py` (empty).
    2. Create `graph_lineage/config_file/commit_msg/lineage_messages.yml` with this exact content:
       ```yaml
       file_modified: "{filename} modified"
       no_changes: "no codebase changes"
       non_critical_changes: "codebase changes, but not in critical files"
       retry: "RETRY FROM {exp_id}"
       resume: "RESUME FROM {exp_id}, checkpoint {ckp_id}"
       critical_files:
         - config.yaml
         - prepare.py
         - train.py
         - requirements.txt
       ```
    3. Create `graph_lineage/config_file/commit_msg/loader.py`:
       - `_DEFAULT_PATH = Path(__file__).parent / "lineage_messages.yml"`
       - `def load_messages(user_path: Path | None = None) -> dict`: load from user_path if exists, else _DEFAULT_PATH. Uses `yaml.safe_load`.
    4. Create `tests/test_messages.py` with tests 1-9 above.
    5. Run tests -- RED.
    6. Create `graph_lineage/diff/description.py`:
       - `def generate_description(changed_files: list[str], strategy: str, messages: dict[str, str] | None = None, exp_id: str | None = None, ckp_id: str | None = None) -> str`
       - If messages is None, call `load_messages()` to get defaults
       - If strategy == "RETRY": return `messages["retry"].format(exp_id=exp_id)`
       - If strategy == "RESUME": return `messages["resume"].format(exp_id=exp_id, ckp_id=ckp_id)`
       - If not changed_files: return `messages["no_changes"]`
       - Filter changed_files to only critical files (from `messages["critical_files"]`)
       - If no critical files in changed: return `messages["non_critical_changes"]`
       - Otherwise: join `messages["file_modified"].format(filename=f)` for each critical changed file, separated by ", "
    7. Run tests -- GREEN.
    8. Update `graph_lineage/diff/__init__.py` to also export `generate_description`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_messages.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f graph_lineage/config_file/commit_msg/lineage_messages.yml` exits 0
    - `test -f graph_lineage/config_file/commit_msg/loader.py` exits 0
    - `test -f graph_lineage/diff/description.py` exits 0
    - `grep -q "yaml.safe_load" graph_lineage/config_file/commit_msg/loader.py` exits 0
    - `grep -q "RETRY FROM" graph_lineage/config_file/commit_msg/lineage_messages.yml` exits 0
    - All 9 tests pass
  </acceptance_criteria>
  <done>YML message templates bundled, loader supports user override, description generator handles all strategy/change combinations. 9 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codebase reconstructor from lineage chain</name>
  <files>graph_lineage/diff/reconstructor.py, tests/test_reconstructor.py</files>
  <read_first>
    - .planning/phases/03-diffmanager-system/03-CONTEXT.md
    - .planning/phases/03-diffmanager-system/03-RESEARCH.md
    - graph_lineage/diff/differ.py
    - graph_lineage/diff/snapshot.py
  </read_first>
  <behavior>
    test_reconstructor.py:
    - Test 1: `apply_unified_diff(original, patch)` correctly applies a simple add/remove patch
    - Test 2: `apply_unified_diff` with empty patch returns original unchanged
    - Test 3: `reconstruct_codebase(chain)` with single base experiment returns full snapshot
    - Test 4: `reconstruct_codebase(chain)` with base + 1 derived correctly applies diff
    - Test 5: `reconstruct_codebase(chain)` with base + 2 derived applies diffs in order
    - Test 6: Round-trip: capture snapshot A, capture snapshot B, compute diff, reconstruct from [A_snapshot, diff] == B content
    - Test 7: Max depth guard (chain > 100) raises ValueError
    - Test 8: New file in diff (file not in base) handled correctly
  </behavior>
  <action>
    1. Create `tests/test_reconstructor.py` with the 8 tests above. For round-trip test (test 6), use `compute_snapshot_diff` from differ.py to create the diff, then reconstruct.
    2. Run tests -- RED.
    3. Create `graph_lineage/diff/reconstructor.py`:
       - `MAX_CHAIN_DEPTH = 100`
       - `def apply_unified_diff(original: str, patch: str) -> str`: Parse unified diff format. Extract hunks (lines starting with @@). For each hunk, apply additions (+) and removals (-), keep context lines (space). If patch is empty string, return original unchanged.
       - `def reconstruct_codebase(chain: list[dict[str, dict[str, str]]]) -> dict[str, str]`:
         - chain[0]["codebase"] = base full snapshot (dict[str, str] of filename -> content)
         - chain[1..n]["codebase"] = derived diffs (dict[str, str] of filename -> unified_diff)
         - Validate len(chain) <= MAX_CHAIN_DEPTH, raise ValueError if exceeded
         - Start with `current = dict(chain[0]["codebase"])`
         - For each derived: iterate its diff dict, apply_unified_diff per file. If file not in current (new file), extract content from diff.
         - Return final current dict
    4. Run tests -- GREEN.
    5. Update `graph_lineage/diff/__init__.py` to also export `reconstruct_codebase`, `apply_unified_diff`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_reconstructor.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f graph_lineage/diff/reconstructor.py` exits 0
    - `grep -q "apply_unified_diff" graph_lineage/diff/reconstructor.py` exits 0
    - `grep -q "reconstruct_codebase" graph_lineage/diff/reconstructor.py` exits 0
    - `grep -q "MAX_CHAIN_DEPTH" graph_lineage/diff/reconstructor.py` exits 0
    - All 8 tests pass
  </acceptance_criteria>
  <done>Reconstructor walks lineage chain applying diffs sequentially. apply_unified_diff parses unified format. Max depth guard prevents infinite chains. Round-trip test proves correctness. 8 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User YML -> loader | User-provided YML file parsed by yaml.safe_load |
| Diff chain -> reconstructor | Chain data from Neo4j applied to reconstruct files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T (Tampering) | loader.py | mitigate | Use yaml.safe_load (not yaml.load) to prevent arbitrary code execution from user YML |
| T-03-05 | D (DoS) | reconstructor.py | mitigate | MAX_CHAIN_DEPTH=100 prevents infinite loop on circular chains |
| T-03-06 | T (Tampering) | reconstructor.py | accept | Malicious diffs could produce incorrect reconstruction; accepted because Neo4j is trusted store |
</threat_model>

<verification>
.venv/bin/pytest tests/test_messages.py tests/test_reconstructor.py -x -v
</verification>

<success_criteria>
- YML message templates load correctly with user override
- Description messages generated for all strategy/change combinations
- Codebase reconstruction works via lineage chain traversal
- Round-trip test (snapshot -> diff -> reconstruct) passes
- All tests pass (17+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-diffmanager-system/03-02-SUMMARY.md`
</output>
