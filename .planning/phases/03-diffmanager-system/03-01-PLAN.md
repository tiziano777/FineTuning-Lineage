---
phase: 03-diffmanager-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graph_lineage/data_classes/neo4j/nodes/experiment.py
  - graph_lineage/diff/__init__.py
  - graph_lineage/diff/snapshot.py
  - graph_lineage/diff/differ.py
  - tests/test_experiment_model.py
  - tests/test_snapshot.py
  - tests/test_differ.py
autonomous: true
requirements: [R3.1, R3.2]

must_haves:
  truths:
    - "Experiment model has base:bool, *_hash fields, fixed bugs"
    - "CodebaseSnapshot captures 4 critical files as frozen Pydantic model"
    - "Differ generates unified diffs between two snapshots"
    - "SHA-256 hashes computed per critical file with CRLF normalization"
    - "Change detection returns list of changed filenames"
  artifacts:
    - path: "graph_lineage/data_classes/neo4j/nodes/experiment.py"
      provides: "Fixed Experiment model with base, *_hash fields"
      contains: "base: bool"
    - path: "graph_lineage/diff/snapshot.py"
      provides: "CodebaseSnapshot frozen Pydantic model"
      exports: ["CodebaseSnapshot", "capture_snapshot"]
    - path: "graph_lineage/diff/differ.py"
      provides: "Unified diff generation + hash computation + change detection"
      exports: ["compute_unified_diff", "compute_file_hash", "detect_changes", "compute_snapshot_diff"]
  key_links:
    - from: "graph_lineage/diff/differ.py"
      to: "graph_lineage/diff/snapshot.py"
      via: "imports CodebaseSnapshot"
      pattern: "from.*snapshot.*import.*CodebaseSnapshot"
---

<objective>
Fix Experiment model bugs and field renames, then implement CodebaseSnapshot and Differ modules -- the core data capture and comparison layer of the DiffManager system.

Purpose: Establish the foundational diff infrastructure that all other Phase 3 modules depend on (messages and reconstruction).
Output: Working snapshot capture, unified diff generation, hash-based change detection, and corrected Experiment model.
</objective>

<execution_context>
@/Users/T.Finizzi/repo/FineTuning-Lineage/.claude/get-shit-done/workflows/execute-plan.md
@/Users/T.Finizzi/repo/FineTuning-Lineage/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-diffmanager-system/03-CONTEXT.md
@.planning/phases/03-diffmanager-system/03-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From graph_lineage/data_classes/neo4j/nodes/base.py:
```python
class BaseEntity(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)
```

From graph_lineage/data_classes/neo4j/edges/derived_from.py:
```python
class DerivedFrom(BaseModel):
    source_exp_id: str = Field(..., min_length=1)
    target_exp_id: str = Field(..., min_length=1)
    diff_patch: dict[str, Any] = Field(default_factory=dict)
```

From graph_lineage/config_file/data_classes/experiment_config.py:
```python
class ExperimentConfig(BaseModel):
    id: str | None = None
    previous_experiment_id: str | None = None
    base_experiment_id: str | None = None
    base: bool = True
    name: str = Field(..., min_length=1)
    description: str = ""
    uri: str = Field(..., min_length=1)
    status: str | None = None
    checkpoint_resume_from: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Experiment model -- bugs + field renames + add base field</name>
  <files>graph_lineage/data_classes/neo4j/nodes/experiment.py, tests/test_experiment_model.py</files>
  <read_first>
    - graph_lineage/data_classes/neo4j/nodes/experiment.py
    - graph_lineage/data_classes/neo4j/nodes/base.py
    - .planning/phases/03-diffmanager-system/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `from typing import Optional` is used (not `from git import Optional`)
    - Test 2: `Experiment()` instantiation succeeds with `codebase` defaulting to empty dict (not string)
    - Test 3: `Experiment` has field `base` of type `bool` defaulting to `True`
    - Test 4: `Experiment` has fields `config_hash`, `prepare_hash`, `train_hash`, `requirements_hash` (all str, default "")
    - Test 5: `Experiment` does NOT have old fields `config`, `prepare`, `train`, `requirements`
    - Test 6: Full instantiation with all new fields works correctly
  </behavior>
  <action>
    1. Create tests/test_experiment_model.py with the 6 tests above.
    2. Run tests -- they MUST fail (RED).
    3. Modify graph_lineage/data_classes/neo4j/nodes/experiment.py:
       - Fix line 5: `from git import Optional` -> `from typing import Optional`
       - Fix line 20: `codebase: dict = Field("")` -> `codebase: dict = Field(default_factory=dict, description="base=True: full snapshot dict[str, str]; base=False: unified diff dict")`
       - Add after line 13 (uri field): `base: bool = Field(True, description="True for base experiment, False for derived")`
       - Rename field `config` -> `config_hash` with description "SHA-256 of config.yaml", keep str type, default ""
       - Rename field `prepare` -> `prepare_hash` with description "SHA-256 of prepare.py", keep str type, default ""
       - Rename field `train` -> `train_hash` with description "SHA-256 of train.py", keep str type, default ""
       - Rename field `requirements` -> `requirements_hash` with description "SHA-256 of requirements.txt", keep str type, default ""
    4. Run tests -- they MUST pass (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_experiment_model.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from typing import Optional" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "default_factory=dict" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "base: bool" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "config_hash" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "prepare_hash" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "train_hash" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -q "requirements_hash" graph_lineage/data_classes/neo4j/nodes/experiment.py` exits 0
    - `grep -c "config\|prepare\|train\|requirements" graph_lineage/data_classes/neo4j/nodes/experiment.py` shows only *_hash variants (no old bare field names)
  </acceptance_criteria>
  <done>Experiment model has all bug fixes applied, 4 fields renamed to *_hash, base:bool added, all 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CodebaseSnapshot and Differ modules</name>
  <files>graph_lineage/diff/__init__.py, graph_lineage/diff/snapshot.py, graph_lineage/diff/differ.py, tests/test_snapshot.py, tests/test_differ.py</files>
  <read_first>
    - .planning/phases/03-diffmanager-system/03-RESEARCH.md
    - .planning/phases/03-diffmanager-system/03-CONTEXT.md
    - graph_lineage/data_classes/neo4j/nodes/experiment.py
  </read_first>
  <behavior>
    test_snapshot.py:
    - Test 1: CodebaseSnapshot is frozen (immutable) -- assigning to `.files` raises ValidationError
    - Test 2: `capture_snapshot(path)` reads 4 critical files into dict[str, str]
    - Test 3: Missing critical file -> stored as empty string ""
    - Test 4: `snapshot.file_hash(filename)` returns SHA-256 hex digest
    - Test 5: `snapshot.hashes()` returns dict of all 4 file hashes

    test_differ.py:
    - Test 1: `compute_file_hash(content)` returns SHA-256, normalizes CRLF to LF
    - Test 2: `compute_unified_diff(old, new, filename)` returns unified diff string with a/b prefixes
    - Test 3: Identical content -> empty diff string
    - Test 4: `detect_changes(old_hashes, new_hashes)` returns list of changed filenames
    - Test 5: No changes -> empty list
    - Test 6: `compute_snapshot_diff(old_snap, new_snap)` returns dict[str, str] of filename -> unified_diff (only changed files)
  </behavior>
  <action>
    1. Create `graph_lineage/diff/__init__.py` exporting: `CodebaseSnapshot`, `capture_snapshot`, `compute_unified_diff`, `compute_file_hash`, `detect_changes`, `compute_snapshot_diff`.
    2. Create `tests/test_snapshot.py` with the 5 snapshot tests.
    3. Create `tests/test_differ.py` with the 6 differ tests.
    4. Run tests -- RED.
    5. Create `graph_lineage/diff/snapshot.py`:
       - `CRITICAL_FILES = ["config.yaml", "prepare.py", "train.py", "requirements.txt"]`
       - `CodebaseSnapshot(BaseModel)` with `model_config = {"frozen": True}`, `files: dict[str, str] = Field(default_factory=dict)`
       - Method `file_hash(self, filename: str) -> str`: SHA-256 of `self.files.get(filename, "")` with CRLF normalization
       - Method `hashes(self) -> dict[str, str]`: all file hashes
       - Function `capture_snapshot(codebase_root: Path) -> CodebaseSnapshot`: reads CRITICAL_FILES from disk, missing files -> ""
    6. Create `graph_lineage/diff/differ.py`:
       - `compute_file_hash(content: str) -> str`: normalize CRLF, SHA-256 hex
       - `compute_unified_diff(old_content: str, new_content: str, filename: str) -> str`: uses `difflib.unified_diff` with `fromfile=f"a/{filename}"`, `tofile=f"b/{filename}"`
       - `detect_changes(old_hashes: dict[str, str], new_hashes: dict[str, str]) -> list[str]`: filenames where hash differs
       - `compute_snapshot_diff(old_snapshot: CodebaseSnapshot, new_snapshot: CodebaseSnapshot) -> dict[str, str]`: returns {filename: unified_diff} for changed files only
    7. Run tests -- GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_snapshot.py tests/test_differ.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f graph_lineage/diff/__init__.py` exits 0
    - `test -f graph_lineage/diff/snapshot.py` exits 0
    - `test -f graph_lineage/diff/differ.py` exits 0
    - `grep -q "frozen" graph_lineage/diff/snapshot.py` exits 0
    - `grep -q "unified_diff" graph_lineage/diff/differ.py` exits 0
    - `grep -q "sha256" graph_lineage/diff/differ.py` exits 0
    - All 11 tests pass
  </acceptance_criteria>
  <done>CodebaseSnapshot and Differ modules implemented with TDD. Snapshot is frozen, captures 4 files, computes SHA-256 hashes. Differ generates unified diffs, detects changes by hash comparison. 11 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> Snapshot | Reading arbitrary files from user's codebase root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | snapshot.py capture_snapshot | mitigate | Only read files from CRITICAL_FILES list, do not follow symlinks, use Path.resolve() to prevent path traversal |
| T-03-02 | I (Info Disclosure) | snapshot.py | accept | File content is stored in Neo4j which is already a trusted store; no external exposure |
| T-03-03 | D (DoS) | snapshot.py | mitigate | Add max file size check (e.g., 10MB) to prevent memory exhaustion on huge files |
</threat_model>

<verification>
.venv/bin/pytest tests/test_experiment_model.py tests/test_snapshot.py tests/test_differ.py -x -v
</verification>

<success_criteria>
- Experiment model bugs fixed, fields renamed, base:bool added
- CodebaseSnapshot frozen model captures 4 files, computes hashes
- Differ generates unified diffs, detects changes by hash
- All tests pass (17+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-diffmanager-system/03-01-SUMMARY.md`
</output>
