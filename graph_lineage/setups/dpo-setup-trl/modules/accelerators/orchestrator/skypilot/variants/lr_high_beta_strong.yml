# Variant: Higher learning rate + stronger DPO beta
# This file overrides only the keys you want to change.
# All other values are inherited from the base config.yml.
#
# Usage:
#   bash schedules/base_schedule.sh variants/lr_high_beta_strong.yml

experiment:
  id: ex2-dpo-lora-0.4-lr5e5-beta03

model:
  training:
    learning_rate: 5e-05
    beta: 0.3
